spring:
  cloud:
    config:
      name: ${CONFIG_SERVER:integration-config-server}
      uri: ${CONFIG_SERVER_URI:http://localhost:7070}

management:
  endpoints:
    web:
      exposure:
        include: refresh